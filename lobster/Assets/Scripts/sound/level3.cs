using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class level3 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        BgmManager.PlaySound2("level3");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
